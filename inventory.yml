all:
  children:
    postgres:
      hosts:
        pihole:
          pg_port: 5432
          pg_primary: true
          cluster_name: "main"
        clusterpi00:
          pg_port: 5433
          pg_primary: false
          cluster_name: "pihole"
    clusterpi:
      hosts:
        mediapi:
        clusterpi00:
          _dphys_swap_size: 600
        clusterpi01:
        clusterpi02:
        clusterpi03:
        clusterpi04:
        clusterpi05:
        clusterpi06:
        clusterpi07:
    remote:
      hosts:
        mountains:
          promtail_loki_push_host: "remote.tswartz.net:3100"
